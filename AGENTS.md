# AGENTS.md

## 專案目標

本專案是台北車站 K 區地下街室內導航系統，包含正式導航 App、Wi-Fi 指紋採集 App、管理後台、Node.js 後端與 Azure SQL 資料庫。

所有修改都必須以「現場可操作、資料不遺失、兩個樓層可連續導航」為優先。不要只完成畫面；後台、API、資料庫、正式 App 與採集 App 必須能連貫運作。

## 使用語言與溝通

- 使用繁體中文說明進度、結果與操作步驟。
- 文字要直接、具體，避免空泛宣傳、過度分段或制式化語句。
- 使用者看不到終端輸出，完成後要交代重要數字、版本、測試結果與檔案位置。
- 不在程式、文件、提交訊息或介面中提及 AI、Codex、提示詞或生成過程。
- 不宣稱現場定位正確，除非已完成對應的實地測試；API 測試與現場驗證要分開陳述。

## 強制品質管理員

每次工作都要安排一輪獨立的自我批判，角色名稱為「品質管理員」。品質管理員不得沿用實作者的樂觀結論，而要站在產品負責人、維運人員、審查者與第一次使用者的角度重新檢查。

品質管理員至少要回答：

- 這次修改是否真的解決使用者問題，還是只改了畫面或暫時繞過問題？
- 後台、API、資料庫、正式 App 與採集 App 是否仍使用一致資料？
- F1、F2、既有點位、搜尋、禁入區、跨樓層連接或定位模型是否可能退化？
- 網路中斷、權限拒絕、後端逾時、資料為空、重複操作與 App 重開後會發生什麼？
- 是否有資料遺失、錯誤覆蓋、隱私、資安、效能、耗電或維護風險？
- 測試是否只驗證成功路徑？還缺少哪些失敗路徑、真機或現場測試？
- 這個版本是否真的可以交給一般使用者，而不是只能在開發者手機上運作？

每次交付前都要執行此檢查。發現可立即修正的問題就先修正再重測；無法在本次完成的問題要在結果中清楚列為「尚未完成」或「上架阻擋」，不得隱藏、淡化或把推測寫成完成。

最終回覆至少要包含簡短的品質管理員結論：已檢查範圍、剩餘風險、是否阻擋發布。小型純文件修改可縮短，但不可省略判斷。

## 專案根目錄

主要 Git 專案位於本文件所在目錄。不要修改 `outputs` 下的歷史備份或工作區外層的舊 Android 範例，除非使用者明確指定。

主要路徑：

- `server.js`：HTTP API、搜尋、定位、導航與下載入口。
- `lib/`：Azure SQL、Firebase 相容層、路線、評論與定位資料存取。
- `public/index.html`：Web 正式導航介面。
- `public/admin.html`：管理後台。
- `android/IndoorNavigation/`：Android 正式版與採集版共用專案。
- `scripts/`：匯入、回填、稽核、測試與維護工具。
- `schema/azure-sql.sql`：Azure SQL 結構。
- `docs/field-test-plan.md`：現場採樣與驗收規範。
- `docs/azure-sql-migration.md`：資料庫建置與移轉說明。

## 系統事實

- 正式導航 App flavor：`navigator`，application id 為 `com.example.indoor.navigator`。
- 指紋採集 App flavor：`collector`，application id 為 `com.example.wififingerprintcollector`。
- Azure SQL 是 Wi-Fi 指紋的主要儲存位置。
- Firebase 只保留評論與尚未完成移轉的小型相容資料，不得當成主要指紋來源重新覆蓋 Azure SQL。
- 正式版、採集版與後台必須使用同一後端資料，不可維護三份互相分離的點位真相。
- Wi-Fi 指紋加權比對是目前可用的主要定位方法。
- AP 三邊定位只有在至少 3 個已校正且穩定的 AP 具有可信座標與距離模型時才可啟用；不可把一般指紋資料誤稱為 AP 三邊定位。
- 紅色 `blocked` 區域是不可走區，路線不得穿越。
- 跨樓層導航必須使用已儲存的 `floor-transitions`，不可只靠相同名稱猜測樓層連接。

## 既有資料基準

以下是最近一次已驗證的生產資料基準，只用來偵測意外遺失；正式判斷仍須查詢目前生產 API：

- Wi-Fi 指紋紀錄：56,701 筆。
- 1F：50,039 筆、57 個指紋點。
- 2F：6,662 筆、8 個指紋點。
- 可搜尋地點：1F 72 個、2F 11 個，共 83 個。
- 跨樓層連接：4 筆，代表 2 組雙向連接。
- 禁入區：1F 15 個、2F 3 個。

若數量低於基準，不得直接視為正常，也不得用較少的本機資料覆蓋生產資料。先找出資料來源、樓層篩選、匯入腳本或 ID 衝突問題。

## 點位與搜尋規則

- 地點可同時具有多個分類，例如「廁所＋販賣機」或「地下停車場＋置物櫃」。正式格式使用 `categories` 陣列，`category` 保留第一個主要分類供舊版相容；所有新增、編輯、搜尋、顯示與評論資格判斷都必須讀取完整分類陣列。
- 所有已存在的 F1/F2 指紋點都必須在後台可見，並可成為正式 App 的搜尋目標。
- 搜尋要涵蓋名稱、關鍵字、備註與分類；相符欄位越多、相似程度越高者排越前面。
- 正式 App 必須同時支援線上 `/api/places` 與打包的離線地點資料，更新後兩者要同步。
- 保留使用者命名的地點，例如店家、樓梯、手扶梯、電梯、廁所、廣場與出口。
- `P01`、`P02` 等測試或舊假名稱不可重新匯入為正式點位。
- 產生點位 ID 時必須包含樓層或其他穩定範圍，避免 1F 與 2F 相同 point ID 互相覆蓋。
- 刪除點位、指紋或樓層資料前，必須先備份並列出預計刪除的筆數、樓層、日期與條件。
- 不可使用座標相近作為跨樓層去重條件；樓層是點位身分的一部分。

## 採集 App 行為

- 開啟地圖時不可因點擊地圖而任意新增點位；只能透過「新增點位」模式建立。
- 已有點位不可任意拖動；只能透過「移動點位」模式修改。
- 即時定位、採樣、AP 校正與錯誤回報要有清楚且互不混淆的流程。
- 已儲存的名稱、分類、關鍵字、備註、營業時間與跨樓層連接狀態，重新開啟時必須回填。
- 跨樓層選項只在勾選跨樓層連接點後顯示；已連接的點重新開啟時必須維持勾選。
- AP 校正要記錄 BSSID、樓層、座標、量測距離、樣本數、穩定度、最後觀測時間與狀態，並可標示位移、停用或需要重測。
- 上傳失敗不得顯示成功；未上傳資料必須留在手機並可重試。

## 正式導航 App 行為

- App 開啟後可自動定位，但不可因背景更新反覆重設使用者的縮放、平移或手動樓層選擇。
- 一般定位與導航定位應使用同一份定位結果，導航模式不可另外凍結或任意推進位置。
- 定位更新間隔可依成功率與延遲調整，不固定要求 1.5 秒；不得快到造成後端壓力，也不得慢到影響行走導航。
- 顯示路線時使用靜態線；開始導航後才可顯示路線動畫。
- 導航箭頭必須貼合可走路線，且不可穿越紅色禁入區。
- 使用者偏離、折返、去廁所或暫停時，定位與路線必須能重新計算，不可永久鎖在舊進度。
- 樓層切換需有遲滯與可信連接點判定，避免剛切換後立即跳回；開始導航時則要先回到定位結果所屬樓層。
- 距離必須使用地圖校正比例換算為公尺；跨樓層只增加已定義的轉換成本，不可把像素直接顯示為公尺。
- 搜尋結果中的距離與導航剩餘距離必須使用一致的單位與比例。

## 定位與資料加權

- 不要用固定每秒前進距離取代真實定位。
- 指紋比對可使用訊號相似度、AP 重疊、樣本數、穩定 AP、資料新鮮度與高信任錨點；新資料較有權重，但不可讓舊而穩定的點自動失效。
- IMU、方向、步數與氣壓只能作為輔助約束，不能蓋過明確的高信任 Wi-Fi 結果。
- 平滑要抑制不合理跳點，但必須允許真實前進、停止與折返。
- 地圖匹配只可投影到可走區或路線；不可把定位吸附穿過牆壁或禁入區。
- 氣壓樓層判定要考慮列車活塞效應與 HVAC 擾動，需使用時間窗、移動狀態與樓層連接點交叉確認。
- 修改任何權重、門檻或平滑規則時，要記錄舊值、新值、理由及可重現測試結果。

## 後台管理要求

- 後台必須能查看與管理地點、指紋點、AP 校正、路線節點、路線邊、禁入區、跨樓層連接、模型、回報與資料庫狀態。
- 已存資料在重新整理後必須仍然存在；只改畫面狀態不算完成。
- 狀態、營業時間、名稱、分類、關鍵字與備註的修改必須寫回後端。
- 分類使用下拉選單，並允許新增分類。
- 管理員執行匯入、清理、回填或模型訓練後，要顯示成功數、跳過數、失敗數與作用範圍。

## 開發規範

- 先閱讀現有程式與資料流，再依既有模式修改；不要另外建立重複的資料來源或平行系統。
- 使用結構化解析與資料存取函式，避免以脆弱的字串替換處理 JSON、CSV 或 SQL。
- 編輯保持小範圍，禁止順手重構無關模組。
- 不得刪除或覆蓋使用者既有變更。
- 手動編輯使用 patch；產物、格式化與正式建置可使用既有命令。
- 程式註解只說明必要的業務理由與非直觀限制，不寫開發過程旁白。
- 使用 ASCII 識別字；使用者介面與專案文件可使用繁體中文。
- 不得把密碼、連線字串、服務帳號 JSON、管理金鑰或個資提交到 Git。

## 常用命令

後端：

```powershell
npm install
npm start
npm run quality
npm run smoke
npm run backfill:fingerprint-places
```

Android：

```powershell
cd android/IndoorNavigation
.\gradlew.bat assembleNavigatorDebug assembleCollectorDebug
```

Azure SQL 匯入需使用互動式密碼腳本，不得把密碼寫進命令列、檔案或聊天紀錄：

```powershell
powershell -ExecutionPolicy Bypass -File scripts/run-azure-sql-seed.ps1
```

## 必做驗證

一般程式修改至少執行：

1. `npm run quality`
2. `npm run smoke`
3. 修改 Android 時執行 `assembleNavigatorDebug assembleCollectorDebug`
4. 檢查 `git diff --check`
5. 檢查 `git status --short`，不得把日誌、帳密、暫存檔或無關產物提交

資料、點位、定位或路線修改還必須驗證：

- `/api/health`
- `/api/storage/status`
- `/api/places?mapId=k-area-airport`
- `/api/wifi-scans/summary` 與 `/api/wifi-scans/points` 的 1F/2F 數量
- `/api/models?mapId=k-area-airport` 的 1F/2F 啟用模型
- `/api/route-zones` 的紅色禁入區
- `/api/floor-transitions` 的雙向樓層連接
- 1F 與 2F 各做一次實際定位 API 測試
- 1F 到 2F、2F 到 1F 各做一次路線測試
- 正式 App 能搜尋全部後台地點
- 採集 App 能顯示全部既有指紋點

Smoke test 會建立測試資料，必須確認腳本有清理自己的測試紀錄，不得污染 K 區正式資料。

## 資料變更安全

- 匯入應可重複執行且不產生重複資料。
- 匯入前後要比較總筆數、樓層數、點位數、最新時間與 BSSID 數量。
- 清除舊資料要使用明確條件，例如 map、floor、point、日期範圍與 session；禁止無條件清空正式表。
- 新模型只有在離線評估優於現行模型，且 1F/2F 實測通過後才能啟用。
- 生產資料異常時，先停止寫入或回填，再從最近可靠備份復原；不要用猜測資料補齊。

## 發布與交付

- 每次 Android 行為變更都要遞增對應 flavor 的 `versionCode` 與 `versionName`。
- 正式 APK 與採集 APK 必須分別建置，不可用同一 application id 覆蓋。
- 發布後確認以下下載端點可取得最新檔案：
  - `/download/navigator-apk`
  - `/download/apk`
- 比對本機與線上 APK 的 SHA-256；雜湊不一致代表部署尚未完成。
- 確認 Render `/api/health` 正常後，再驗證生產資料與搜尋。
- Git 提交訊息使用簡短祈使句，描述實際改動；一個提交只包含同一目的的變更。

## 正式上架導向

開發目標不是長期停留在 debug APK，而是能產出可維護、可監控、可送審與可供一般使用者安裝的正式產品。任何「最新版」都要區分開發測試版與正式候選版。

正式候選版至少要符合：

- 使用 release build 與正式 application id，透過受保護的正式簽署金鑰簽名；簽署金鑰、密碼與憑證不得提交 Git。
- `versionCode` 必須遞增，`versionName`、後端版本與資料版本要可追溯。
- 正式版不得依賴 `localhost`、個人電腦、debug 開關、測試帳號、硬編碼密碼或臨時通道。
- 後端使用 HTTPS，生產與測試資料隔離；服務重啟後資料不能遺失。
- Wi-Fi、定位、感測器、通知、相機或檔案等權限只在功能需要時請求，並提供拒絕後可理解的處理方式。
- 提供與實際資料流一致的隱私權政策，說明 Wi-Fi BSSID、定位、裝置識別、評論、SOS 收件人與保留期限；可刪除的個人資料必須有刪除方式。
- SOS 必須明確說明不是官方緊急救援服務，收件人由使用者自行設定，不可在未同意時上傳或分享位置與圖片。
- 不得把個資、完整定位紀錄、服務帳號、管理金鑰或資料庫錯誤細節輸出到公開日誌。
- 具備離線、弱網、逾時、重試、後端維護與資料尚未同步時的可理解狀態，不可無限載入或誤報成功。
- 完成常用 Android 尺寸、目標 Android 版本、冷啟動、背景切回、旋轉、低記憶體與長時間導航測試。
- 檢查啟動速度、地圖載入、定位更新耗電、網路請求頻率、當機與 ANR；有明顯問題時不得標為可上架。
- 上架前依當時最新的 Google Play 政策檢查目標 API、資料安全表單、權限聲明、內容分級、隱私權網址與測試要求，不可沿用過期規則。
- 準備商店名稱、正式圖示、截圖、功能說明、隱私權網址、支援聯絡方式、版本說明與測試帳號說明（若需要）。
- 正式發布採分階段方式，保留可回復的上一版 APK/AAB、後端版本、資料庫備份與回滾步驟。

若仍使用 debug APK、未建立正式簽署、缺少隱私權政策、核心資料尚未一致、真機導航未驗收或仍存在資料遺失風險，品質管理員必須判定為「不可正式上架」，即使功能展示可以運作。

## 完成定義

工作只有在下列條件都滿足時才算完成：

- 功能已實作，不只是提出方案。
- 後端與資料庫確實保存，重新整理或重開 App 後仍存在。
- 後台、正式 App 與採集 App 使用一致資料。
- F1/F2 點位、搜尋、禁入區與跨樓層導航沒有退化。
- 自動測試與必要 Android 建置通過。
- 若涉及定位品質，已明確區分模擬/API 驗證與現場驗證。
- 已提供可安裝 APK、管理後台或其他使用者實際需要的交付入口。
- 品質管理員已完成反向檢查，並明確判定此次修改是否阻擋測試發布或正式上架。
- 若工作目標是正式發布，已完成 release 簽署、隱私與權限檢查、真機驗收、商店素材及回滾準備；只有 debug APK 不算完成。
